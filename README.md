# Keyboard-Event-Logger
A Python Tkinter-based GUI application that records keyboard events using the pynput library. It allows users to start and stop logging, view captured keystrokes in real time, and save the recorded logs to a file through a simple and interactive interface.
